pipeline {
  agent any
  
  stages {
    stage('Build') { // Get code
      steps {
        // get code from our Git repository
        git 'https://github.com/rahmanca/DevOps-Demo-WebApp.git'
        }
        post{
        success {
          echo "Success"
          }
       }
      } 
    }
  }
